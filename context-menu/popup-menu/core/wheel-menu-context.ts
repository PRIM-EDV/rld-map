import { Button } from '../../wheel-menu/elements/button.element';
import { Slider } from '../../wheel-menu/elements/elements';

export class WheelMenuContext {
    public position: {x: number, y: number} = {x: 0, y: 0};
    // public sliders: Array<Slider>
}