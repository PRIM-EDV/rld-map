import { Button } from '../elements/button.element';
import { Slider } from '../elements/elements';

export class WheelMenuContext {
    public buttons: Array<Button> = [];
    // public sliders: Array<Slider>
}